# Spec 021 — I1: Demo configuration and the two run targets

**Status: claim stub** — reserves spec number 021 for TASK-0021. The real spec.md,
plan.md, and tasks.md are authored on this branch (task-0021-demo-config) before
implementation dispatches.

Board card: TASK-0021. Plan of record: docs/design/demo-build-plan.md §3.4 (I1).
